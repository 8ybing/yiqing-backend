spring:
  profiles:
    active: prod


#配置日志
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    cache-enabled: true
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: cn.bybing.entity